import { Link } from 'react-router-dom'
import SubNav_ from './styled'

const SubNav = ({data})=>{

    return (

        <SubNav_>
             {data.map((item,index)=><li key ={index}><Link key ={index}to={item.path}>{item.title}</Link></li>)}
        </SubNav_>
    )
};

export default SubNav;